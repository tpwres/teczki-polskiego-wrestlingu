# The URL the site will be built for
base_url = "https://k3rni.github.io/teczki-polskiego-wrestlingu/"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

title = "TPW" # Teczki Polskiego Wrestlingu

theme = "mensch"

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = false

[slugify]
paths_keep_dates = true

[extra]
# Put all your custom variables here

list_pages = false
default_theme = "dark"
mensch_links = [
  { path = "@/a/contributing.md", name = "Contributing", icon="edit" },
  { url = "//github.com/k3rni/teczki-polskiego-wrestlingu", name = "GitHub", icon="github" }
]
mensch_theme = "theme-base-0e"