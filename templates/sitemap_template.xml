<?xml version="1.0" encoding="utf-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{% for entry in entries %}
  <url>
    <loc>${CF_PAGES_URL}}/{{ entry.permalink | escape_xml | safe }}</loc>
    {% if entry.updated %}
    <lastmod>{{ entry.updated }}</lastmod>
    {% endif %}
  </url>
{% endfor %}
</urlset>
